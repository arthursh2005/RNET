1) Restore DB (SQL Server) from file RNET.bak
2) In file App.config change the connection string to the database in the section "connectionStrings".
3) For using hebrew interface you need to write "<add key="LANG" value="he-IL" />" in the section "appSettings".
